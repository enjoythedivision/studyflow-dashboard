# studyflow todo

- [ ] add `authApi.js` for register, login, session, and logout
- [ ] connect signup and login to identity
- [ ] send `credentials: "include"` with authenticated requests
- [ ] remove the fake `localstorage` session
- [ ] assign each course to its authenticated user
- [ ] filter and modify courses by authenticated user id
- [ ] create and apply the ownership migration
- [ ] test with two user accounts
- [ ] replace alerts with inline loading and errors
- [ ] add validation and api tests
- [ ] update the readme
- [ ] deploy